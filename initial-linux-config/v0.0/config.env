# config.env
# This file contains configuration data for the new server.
# Modify the values as needed before running the script.

# New username for the server
NEW_USERNAME="sumon"

# Password for the new user (should be a strong password for security)
# IMPORTANT: This file stores the password in plain text.
# For more secure methods, consider using SSH keys and disabling password authentication.
NEW_USER_PASSWORD="sumon123"

# Hostname for the server
SERVER_HOSTNAME="cloud3"

# Domain name for the server (e.g., example.com)
DOMAIN="paulco.xyz"

# Static IP address and CIDR notation (e.g., 192.168.1.100/24)
STATIC_IP_CIDR="192.168.0.63/24"

# Default Gateway
GATEWAY="192.168.0.1"

# DNS Servers (comma-separated, e.g., "8.8.8.8,8.8.4.4")
DNS_SERVERS="8.8.8.8,8.8.4.4"

# Network Interface to configure (e.g., enp0s3, eth0).
# If left empty (NETWORK_INTERFACE=""), the script will attempt to auto-detect the primary interface.
# If you specify a value, the script will use that interface.
NETWORK_INTERFACE="" # Leave empty for auto-detection, or specify like "enp0s3"

# System Timezone (e.g., Asia/Dhaka, America/New_York, Europe/London)
# You can find a list of valid timezones by running 'timedatectl list-timezones' on an Ubuntu system.
TIMEZONE="Asia/Dhaka"

# Essential packages to install (space-separated)
PACKAGES_TO_INSTALL="git curl wget vim htop net-tools openssh-server python3-pip"
